{
  "//": "Development-only overrides: run against a local SQLite file instead of SQL Server and seed a throwaway admin. NOTE: password rotation is ENABLED here too — 10s after startup the seeded password stops working; read App_Data/current-api-password.json (or the console log) for the current one. None of these values are safe for production — appsettings.json + environment variables carry the real configuration.",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Information"
    }
  },
  "ConnectionStrings": {
    "Registry": "Data Source=registry-dev.db"
  },
  "Database": {
    "Provider": "Sqlite",
    "ApplyMigrationsOnStartup": true
  },
  "Auth": {
    "SeedAdmin": {
      "Username": "admin",
      "Password": "ChangeMe!123",
      "DisplayName": "Local Development Admin"
    },
    "PasswordRotation": {
      "Enabled": true,
      "IntervalMinutes": 45,
      "InitialDelaySeconds": 10,
      "LogPasswords": true,
      "WriteStateFile": true
    }
  }
}
