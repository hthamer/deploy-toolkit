{
  "//": "Development-only overrides: run against a local SQLite file instead of SQL Server, seed a throwaway admin, use a throwaway JWT signing key. None of these values are safe for production — appsettings.json + environment variables carry the real configuration.",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Information"
    }
  },
  "ConnectionStrings": {
    "Registry": "Data Source=registry-dev.db"
  },
  "Database": {
    "Provider": "Sqlite",
    "ApplyMigrationsOnStartup": true
  },
  "Auth": {
    "SeedAdmin": {
      "Username": "admin",
      "Password": "ChangeMe!123",
      "DisplayName": "Local Development Admin"
    },
    "Jwt": {
      "SigningKey": "dev-only-signing-key-0123456789abcdef-DO-NOT-USE-IN-PROD"
    }
  }
}
